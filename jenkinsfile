node('UBUNTU') {
    stage('vcs') {
        git branch: 'master', url: 'https://github.com/nagarjunaduggireddy/spring-framework-petclinic.git'
    }
    stage("build") {
        sh 'mvn package'
    }
    stage("archive results") {
        junit '**/surefire-reports/*.xml'

    }
}